#include <iostream>
using namespace std;
int main() {  // no redundant indentation if we read from a script
    cout << "Hello, World!" << endl;
    return 0;
}  // the script path and filename is listed below the code block
